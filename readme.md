# Backs up a MongoDB database to S3

### Comes with a helm chart for a K8s cronjob.

### TODO
- optionally use a StatefulSet / PersistentVolumeClaim to also keep a local backup
- reporting / alerting
